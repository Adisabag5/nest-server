# Copy to .env and fill in real values. .env is gitignored; this file is the schema.

PORT=3000

# DB
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change-me
DB_NAME=nest_server

# Auth
# openssl rand -base64 48
JWT_SECRET=replace-me-with-at-least-32-characters-of-random
JWT_EXPIRES_IN=1h
